<header class="my-4 flex justify-between items-center">
  <h1 class="text-2xl font-bold text-gray-500">My Todo-list</h1>
  
  <% if (locals.user) { %>
    <div class="text-sm flex items-center gap-2">
      <p><%= user.firstName?.toUpperCase() %></p>
      <a class="text-blue-400" href="/signout">Sign Out</a>
    </div>
  <% } %>
</header>
